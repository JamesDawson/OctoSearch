FROM trevorsullivan/powershell

RUN sudo apt install nuget && \
    mkdir -p /app/build
COPY . /app/build/
RUN rm /app/build/Dockerfile*

VOLUME /app/src
VOLUME /output

WORKDIR /app/build
CMD ["powershell", "-File", "build.ps1"]